# Enable workspaces
workspaces=true
# Disable telemetry to prevent permission issues
telemetry=false
# Ensure proper cache behavior
cache=.npm-cache
# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true 